{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "title": "Contribution review",
  "description": "Please consider carefully whether this data request should be accepted, rejected (with the possibility of resubmission) or rejected outright given the contribution specified therein.<br/><br/>The specified contribution can be found at the bottom of the datarequest form (i.e. at the bottom of this page).<br/><br/>",
  "properties": {
    "decision": {
      "type": "string",
      "title": "In light of the specified contribution, this data request is:",
      "enum": [
        "Accepted",
        "Rejected (resubmit)",
        "Rejected"
      ]
    }
  },
  "dependencies": {
    "decision": {
      "oneOf": [
        {
          "properties": {
            "decision": {
              "enum": [
                "Accepted"
              ]
            }
          }
        },
        {
          "properties": {
            "decision": {
              "enum": [
                "Rejected (resubmit)",
                "Rejected"
              ]
            },
            "feedback_for_researcher": {
              "type": "string",
              "title": "Feedback for researcher",
              "description": "Please provide feedback to the researcher in case of rejection here. This feedback will be included in the rejection email."
            }
          },
          "required": [
            "feedback_for_researcher"
          ]
        }
      ]
    }
  },
  "required": [
    "decision"
  ]
}
